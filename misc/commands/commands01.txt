likes = pd.read_csv("~/data/training/relation/relation.csv")import pandas as pdlikes = pd.read_csv("~/data/training/relation/relation.csv")profiles = pd.read_csv("~/data/training/profile/profile.csv")likesDF = pd.DataFrame(likes)profilesDF = pd.DataFrame(profiles)tempDF = pd.get_dummies(likesDF['userid'])tempDF = pd.get_dummies(likesDF['like_id'])print(likesDF)likesDF = pd.DataFrame(likes)print(likesDF)likes = pd.read_csv("~/data/training/relation/relation.csv")print likesprint(likes)print(likes[2])print(likes[:,2])print(likes(:,2))print(likes[[:],[2]])print(likes[:,2])test = likes[:,2]test = likesDF[:,2]test = likesDF[[:,2]]print(likes.ix[:,2])print(likes)print(likes.ix[:,1])likesUIDs = likes.ix[:,1]print(likesUIDs)likesLIDs = likes.ix[:,2]print(likesLIDS)print(likesLIDs)pd.to_array(likesUIDs)likesUIDs = likes.ix[:,1].valuesprint(likesUIDs)likesLIDs = likes.ix[:,2].valuesdfLikes = pd.DataFrame({'userid': likesUIDs, 'like_id': likesLIDs}, index='userid')dfLikes = pd.DataFrame({'userid': likesUIDs, 'like_id': likesLIDs}, index=inde)dfLikes = pd.DataFrame({'userid': likesUIDs, 'like_id': likesLIDs}, index=index)dfLikes = pd.DataFrame({'userid': likesUIDs, 'like_id': likesLIDs})print(dfLikes)print(dfLikes.columns.tolist())dfLikes = pd.DataFrame({'userid': likesUIDs, 'like_id': likesLIDs}, index=likesUIDs)print(dfLikes)dfLikes = pd.DataFrame({'like_id': likesLIDs}, index=likesUIDs)print(dfLikes)print(dfLikes.columns.tolist())print(profiles)print(profiles.columns.tolist())profUIDs = profiles.ix[:,1].valuesprint(profUIDs)profAGE = profiles.ix[:,2].valuesprofSEX = profiles.ix[:,3].valuesprofOPE = profiles.ix[:,4].valuesprofCON = profiles.ix[:,5].valuesprofEXT = profiles.ix[:,6].valuesprofAGR = profiles.ix[:,7].valuesprofNEU = profiles.ix[:,8].valuesdfProfs = pd.DataFrame({'age': profAGE, 'gender': profSEX, 'ope': profOPE, 'con': profCON, 'ext': profEXT, 'agr': profAGR, 'neu': profNEU}, index=profUIDs)print(dfProfs)print(dfProfs.columns.tolist())help()keywordstopicsqhelp()topicsqhistLEN = readline.get_current_history_length()import readlinehistLEN = readline.get_current_history_length()print(histLEN)Hist = []for i in range(0, histLEN):print(readline.get_history_item(0))print(readline.get_history_item(1))histLEN = readline.get_current_history_length()for i in range(histLEN):	Hist.append(readline.get_history_item(i+1)histLEN = readline.get_current_history_length()