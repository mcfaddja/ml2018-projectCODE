import pandas as pd
import numpy as np
likes = pd.read_csv('~/data/training/relation/relation.csv')
likesUID = likes.ix[:,1].values
likesLID = likes.ix[:.2].values
print(likesUID)
print(likesLID)
likesLID = likes.ix[:,2].values
print(likesLID)
dfLikes = pd.DataFrame({'userid': likesUID, 'like_id':likesLID})
aTMPdf = pd.get_dummies(dfLikes[['userid']], columns=dfLikes[['like_id']])
aTMPdf = pd.get_dummies(dfLikes['userid'], columns=dfLikes['like_id'])
print(aTMPdf)
aTMParray = aTMPdf.ix[:,1:].values
print(len(aTMParray))
print(aTMParray.shape)
aTMParray = aTMPdf.ix[:,:].values
print(aTMParray.shape)
print(aTMParray[0,:])
aTMPdf.to_csv('/media/ThruOptHDD/lidVSuid.csv')
f = open('/home/ubuntu/command4.txt', 'w')
import readline
for i in range(readline.get_current_history_length()):
	f.write(readline.get_history_item(i+1) + '\n')
